# ── Development ──────────────────────────────────────────────────────────────
# This file is picked up automatically by react-scripts (Create React App).
# Variables MUST start with REACT_APP_ to be embedded in the build.
# Restart the dev server after editing this file.

REACT_APP_API_BASE_URL=http://localhost:9191/invent
